## Summary

Full site UI polish across services, discovery, landing, terminal, and docs pages.

### Services page
- Provider column, horizontal filter tags, skeleton loader
- Search dropdown with smart results (services, endpoints, categories), arrow-key nav, Cmd+K
- Colored method badges (GET/POST/PUT/DELETE/PATCH), anchor URLs with deep-link support
- Intent badge next to endpoint route, em dash for no-cost endpoints

### Discovery
- ASCII MPP logo with animated block-character art, themed light/dark
- Mobile search UX — centered by default, header on results view
- Dropdown blur background, scroll gradients, no-matches hint
- Endpoint row click: green checkmark confirms copy
- Agent tooltip with install commands, llms.txt link, copy button

### Landing page
- Larger lockup: `clamp(288px, 33.6vw, 456px)`
- Shimmer CTA border + text, entrance animations (skip on revisit)
- CTA hover taglines equalized, full-width divider

### Terminal
- `$` prompt, green `./demo.sh` title and first command
- Full-width CSS border separator, localStorage last-visit timestamp
- Payment channel steps: deposit + req200

### Layout and global styles
- Mobile nav portal with hamburger menu (nav links, not docs sidebar)
- Code block copy button: hover-only, z-index 100
- Page content opacity fade-in on route change
- Muted text 15% brighter, sidebar font 14px on nested items

### Icons and brand
- Re-generated all 44 service icons with consistent sizing
- Icon sync script and updated gen-icons tooling
- Brand page expanded with download button and asset section
- Lockup SVG refresh

## Test plan

- [ ] Services page: provider column on desktop, hidden on mobile/tablet
- [ ] Services page: filter tags horizontal at all widths, ~5 per row on mobile
- [ ] Discovery: search dropdown blur bg, no-matches state
- [ ] Discovery: endpoint table gradients only when scrollable, no layout shift on click
- [ ] Discovery: add-to-agent tooltip stays open when moving cursor to it
- [ ] Landing page: lockup larger, no text overlap on CTA hover, shimmer on scroll CTA
- [ ] Terminal: $ prompt, green ./demo.sh, full-width border, stored last-visit time
- [ ] Global: code copy button hover-only on all doc pages, muted text readable
- [ ] Mobile: hamburger menu shows nav links (not docs sidebar) on services/landing
- [ ] Brand page: download button works with brand.zip
- [ ] Cross-viewport: mobile, tablet, wide desktop
